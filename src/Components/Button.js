import React from 'react'
import PropTypes from 'prop-types'

const Button = ({name}) => {
    return (
        <div>
            
        </div>
    )
}

Button.propTypes = {
name: PropTypes.string
}

export default Button
